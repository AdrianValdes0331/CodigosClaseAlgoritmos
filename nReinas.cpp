//Adrian Valdes Zavala
//A01720439

#include <iostream>
#include <stdio.h>
//Backtracking tiende a ser exponencial
using namespace std;
/*
bool cumple(indice i) {
	indice k;
	bool switch;
	k = 1;
	switch = true;
	while (k<i&&switch)
	{
		if (col[i]==col[k]||abs(col[i]-col[k]) == i-k) {
			switch = false;
			k++;
		}
		return switch;
	}
}

void reinas(indice i) {
	indice j;
	if (cumple(i)) {
		if (i=n) {
			for (int k=i; k<=i;) {
				cout <<
			}
		}
	}
}
*/
int main() {

}